# Weekly timer for the mythos LoRA run. Enable with:
#   systemctl --user enable --now legion-lora.timer
[Unit]
Description=Run Legion mythos LoRA weekly

[Timer]
OnCalendar=Sun 04:00
Persistent=true

[Install]
WantedBy=timers.target
